import 'package:flutter/material.dart';

class VendorsProfilePage extends StatelessWidget {
  const VendorsProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}